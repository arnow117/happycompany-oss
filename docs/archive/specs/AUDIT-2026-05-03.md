# Spec vs Code 一致性审计

> 2026-05-03
> 对照文件：concept-design.md + implementation-plan.md
> 审计范围：C1-C9 / X1-X8 约束、D1-D15 设计决策、S1-S10 测试故事

---

## 一、约束审计

### PASS (13/16)

| ID | 约束 | 说明 |
|----|------|------|
| C1 | IM 操作通过 ChannelAdapter | ✅ feishu.ts/dingtalk.ts 以外无平台 SDK 导入 |
| C3 | Skill 只声明接口 | ✅ skills.ts 纯结构解析，无业务逻辑 |
| C4 | App 三层结构一致性校验 | ✅ `consistency-check.ts` 实现 README ↔ SKILL.md ↔ CLAUDE.md 校验，含 API 端点 |
| C5 | System Dir 与 Workdir 隔离 | ✅ 单向安装流，registry→workdir |
| C6 | 安装后 session 重载生效 | ✅ install handler 调用 `clearSessionsForWorkdir()`，安装后清 session |
| C9 | pino logger，不用 console.log | ✅ src/ 零 console.log |
| X1 | 不引入拓扑 DAG | ✅ bot.ts 注释明确 1:1 routing |
| X2 | 不使用 Docker | ✅ 无 Docker 相关代码 |
| X3 | 不自动生成 App 代码 | ✅ 无 codegen |
| X5 | 不使用 Python 平台代码 | ✅ 平台本身零 Python，Python 仅存在于 App 层（独立进程） |
| X6 | 不引入 fs.watch | ✅ 零 fs.watch |
| X7 | 不硬编码密钥 | ✅ 全部 $ENV_VAR 占位 |
| X8 | 不使用 any 类型 | ✅ src/ 零 `: any` 或 `as any` |

### PARTIAL (2/16)

| ID | 约束 | 问题 | 严重度 |
|----|------|------|--------|
| **C2** | 换绑 = 清 session | ~~无 rebind 检测~~ → **已修复**：`updateOptions()` cwd 变更时自动 `clearAllSessions()` | ~~低~~ ✅ |
| X4 | 不暴露平台特定类型 | ChannelAdapter 接口干净，但 streaming-card.ts 直接 import lark 类型（实现层，非接口层） | 低 |

### UNKNOWN/N/A (1)

| ID | 说明 |
|----|------|
| C8 | `@vitest/coverage-v8` 未安装，无法测量覆盖率 |

> **C7（系统边界校验）**：路径穿越已修复（`safePath()`），IM 消息已加 Zod schema 校验。从 PARTIAL 升级为 PASS。

---

## 二、设计决策审计

### PASS (12/15)

D1, D3, D4, D5, D6, D7, D8, D9, D12, D13, D14, D15

| ID | 决策 | 修复说明 |
|----|------|----------|
| **D4** | skill → CLI → app 调用链 | ✅ `app-runner.ts` 实现 `runAppCli()`、`hasCliEntry()`、`listCliApps()`，结构化 CLI 中间层 |
| **D6** | Bot 描述来源 = workdir CLAUDE.md | ✅ `desc.ts` 新增 `extractBotDescription()` 从 CLAUDE.md 提取 bot 级描述，SKILL.md 继续提供 skill 级能力列表 |
| **D8** | 知识管理 = 独立 App | ✅ `apps/kb-management/` 创建，含 kb-ingest + kb-query、CLI 入口、SKILL.md/CLAUDE.md/README.md |
| **D14** | 双语言 TS + Python | ✅ `apps/python-example/` 参考实现 + `detectAppLanguage()` 语言检测 + Python 一致性警告 |

### PASS (was PARTIAL, upgraded)

| ID | 决策 | 修复说明 |
|----|------|----------|
| **D2** | 换绑 = 清 session | ✅ `updateOptions()` cwd 变更时自动 `clearAllSessions()` |
| **D10** | 下次 session 启动生效 | 代码支持热更新（updateOptions），是 spec 的超集 |
| **D11** | README/CLAUDE/代码三者一等 | ✅ `consistency-check.ts` + API 端点 + README/CLAUDE.md 均有展示路径 |

---

## 三、测试覆盖审计

### PASS (7/10)

S1 飞书私聊、S3 钉钉私聊、S6 App 安装版本、S7 /clear、S8 消息去重

| ID | 故事 | 新增测试 |
|----|------|----------|
| **S9** | 管理后台 App 管理 | ✅ 12 web-app-routes 测试 + 16 web-skill-api 测试 + 13 consistency-check 测试 |

### PARTIAL (3/10)

| ID | 故事 | 缺口 |
|----|------|------|
| S2 | 群聊 @路由 | 缺 multi-bot A/B 测试 + desc 注入验证 |
| S4 | 钉钉群聊文件 | 缺 nonce fenced block 格式验证 |
| S5 | Skill 路由 | 缺端到端测试：skill 发现 → agent 感知 → CLI 调用 → 格式化回复 |
| S10 | 使用统计 | 缺 per-skill 调用次数 + per-bot 消息趋势 |

---

## 四、修复记录

### Commit 9e7fa9e — P0 安全修复
- 路径穿越漏洞：`safePath()` 校验 `../`、绝对路径、null-byte
- 12 测试 + 审计报告

### Commit 429e5ad — 审计修复批次 1
- C6: 安装后清 session（`clearSessionsForWorkdir`）
- D2/C2: rebind 自动清 session（`updateOptions` cwd 变更检测）
- D6: Bot 描述改为 CLAUDE.md（`extractBotDescription`）
- P0: Zod schema 校验（飞书 + 钉钉 webhook payload）
- C4: 一致性校验（`consistency-check.ts` + API 端点）
- 52 新测试

### Commit ac13b80 — CLI 中间层 + 知识管理 App
- D4: `app-runner.ts` 结构化 CLI 执行层
- D8: `apps/kb-management/` 知识管理 App
- 28 新测试

### Commit f7f2507 — Skill 管理 API + Python App 层
- S9: Workdir skill CRUD 端点（list/get/put SKILL.md）
- D14: Python App 支持（`detectAppLanguage` + `apps/python-example/`）
- 31 新测试

---

## 五、剩余待完善

### P2 — 可迭代

- [ ] **Per-skill / per-bot 统计**（S10）：analytics 模块缺 skill 调用追踪和 bot 维度分解
- [ ] **Skill 路由端到端测试**（S5）：验证 skill 发现 → agent 感知 → CLI 调用全链路
- [ ] **Multi-bot 群聊测试**（S2）：同群两个 bot，验证只有 @mentioned 的回复 + desc 注入

### P3 — 测试完善

- [ ] **Nonce fenced block 测试**（S4）：验证文件内容注入的 nonce 格式
- [ ] **安装覆盖率工具**（C8）：`npm i -D @vitest/coverage-v8`，跑一次确认 >= 80%

### P4 — 次要

- [ ] **X4 lark 类型泄漏**：streaming-card.ts import lark 类型。可接受，长期应抽象
