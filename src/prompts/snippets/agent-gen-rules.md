## Rules
- Choose role based on the job description
- Only list tools/skills that are actually needed
- systemPrompt must include: role description, core capabilities, workflow steps, handoff instructions
- Output ONLY valid YAML wrapped in ```yaml ... ```
- Do NOT include any explanation before or after the YAML block
